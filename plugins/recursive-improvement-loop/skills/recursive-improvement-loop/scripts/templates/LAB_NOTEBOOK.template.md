# LAB NOTEBOOK — {{NAME}}

Living document of the optimization loop. Three tiers: INSIGHTS (durable
lessons), GRAVEYARD (one line per dead idea), ACTIVE LOG (last ~15 verbose
iteration blocks; aged blocks roll into the graveyard via
`python3 loop.py compact`).

**Objective**: <maximize|minimize WHAT, measured HOW, on WHAT frozen data>
**Scoring**: <primary metric + which number promotes (e.g. holdout only);
noise bands for any timing metrics; what the hard gate checks>

## INSIGHTS
- <seed with everything already known: data profile facts, prior-art
  numbers, theoretical bounds, constraints. Each bullet = one durable,
  load-bearing fact the next iteration should not have to rediscover.>

## Live lineages
| lineage | status | best | notes |
|---|---|---|---|
| <family-1> | ACTIVE (seed) | — | <the obvious first approach> |
| <family-2> | dormant | — | <a structurally different approach> |
| <family-3> | dormant | — | <the long-shot / frontier approach> |

## Leaderboard snapshot
(maintained by hand from `leaderboard.json` after each iteration)
- Champion: none yet.

## GRAVEYARD
| id | lineage | primary | verdict | why it failed |
|---|---|---|---|---|

## ACTIVE LOG
(append iteration blocks below — newest last; format in PROMPT.md §5)
